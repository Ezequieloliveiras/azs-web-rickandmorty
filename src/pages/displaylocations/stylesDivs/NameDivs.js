import { styled } from '@mui/system'

const Div = styled('div')(() => ({
    display: 'flex',
    flexDirection:'column',
    alignItems:'flex-start',
    width: 'auto',
    paddingBottom: '10px'
}))

export default Div;